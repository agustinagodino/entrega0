function mostrarDescripcion(idDescripcion) {

    // EXTRAEMOS EL ELEMENTO OCULTO
    var parrafo = document.getElementById(idDescripcion);

    // MOSTRAMOS EL ELEMENTO
    parrafo.style.display = "block";
}